{
  "name": "@kiln/tests",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Kiln end-to-end tests — drive the real web client against a running stack (docs/specs/01 §2, 02 §4).",
  "scripts": {
    "test": "playwright test",
    "install-browser": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  },
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": ">=22"
  }
}
